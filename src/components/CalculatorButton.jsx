import { Button } from "@mui/material";

const CalculatorButton = ({
  
}) => {
  return (
    <Button
      variant={variant}
      color={color}
      onClick={onClick}
      
    >
      Children
    </Button>
  );
};

export default CalculatorButton;